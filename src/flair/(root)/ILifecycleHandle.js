/**
 * @name ILifecycleHandle
 * @description ILifecycleHandle interface
 */
$$('ns', '(auto)');
Interface('(auto)', function() {
    // start - async
    this.start = nim;

    // stop - async
    this.stop = nim;

    // restart = async
    this.restart = nim;
});
