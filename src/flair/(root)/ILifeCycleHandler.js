/**
 * @name ILifeCycleHandler
 * @description ILifeCycleHandler interface
 */
$$('ns', '(auto)');
Interface('(auto)', function() {
    this.start = nim;
    this.isStarted = nip;
    this.isReady = nip;
    this.stop = nim;
    this.restart = nim;
});
