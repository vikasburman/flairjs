const { Server } = ns('flair.boot');

/**
 * @name Server
 * @description Default server implementation
 */
$$('ns', '(auto)');
Class('(auto)', Server, function() {
    $$('override');
    this.construct = () => {
    };

    $$('override');
    this.boot = async () => {
    };

    $$('override');
    this.start = async () => {
    };

    $$('override');
    this.stop = async () => {
    };

    $$('override');
    this.ready = async () => {
    };

    $$('override');
    this.dispose = () => {
    };
});
